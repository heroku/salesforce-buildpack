#!/bin/sh

# ensure salesforce buildpack is applicable to source
# REVIEWME: let's re-think this - bit draconian - and repos could be multiple force.com projects (right?)
if [ -f $1/config.json ]; then
  echo "Force.com Application (AC/DX)"
  exit 0
else
  exit 1
fi
